<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto" >
    <item
        android:id="@+id/action_list"
        android:title="@string/list"
        app:showAsAction="never"
        />
    <item
        android:id="@+id/action_grid"
        android:title="@string/grid"
        app:showAsAction="never"
        />
</menu>
<!--
Perhatikan tag <item>, attribute title diberikan untuk menampilkan teks pada menu. Selain itu, terdapat 5 kondisi yang dapat dipasang pada
attribute app:showAsAction:

    ifRoom, akan menampilkan action ketika ada ruangan pada action bar.
    withText, akan menampilkan actionitem beserta judulnya.
    never, tidak akan pernah ditampilkan pada action bar dan hanya akan ditampilkan pada overflow menu.
    always, akan selalu tampil pada action bar.
    collapseActionView, berhubungan dengan komponen collapsible .

Anda juga dapat menampilkan ikon pada App Bar dengan menggunakan attribute android:icon yang dikombinasikan dengan aksi ifRoom untuk
menghasilkan tampilan dengan search bar.

    <item
    android:id="@+id/action_search"
    android:icon="@android:drawable/ic_search_category_default"
    android:title="@string/search"
    app:showAsAction="ifRoom" />
-->